package com.ocp.evalformation.utils

import android.content.Context
import android.net.Uri
import android.util.Log

/**
 * ExcelHelper — placeholder for future Excel import/export
 * Will be reimplemented when the Formation import workflow is defined.
 */
object ExcelHelper {
    private const val TAG = "ExcelHelper"

    fun exportPlaceholder(context: Context): Uri? {
        Log.i(TAG, "Export not yet implemented")
        return null
    }
}
